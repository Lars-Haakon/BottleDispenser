import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import DispenseMachine from './DispenseMachine';

ReactDOM.render(
  <React.StrictMode>
    <DispenseMachine />
  </React.StrictMode>,
  document.getElementById('root')
);
